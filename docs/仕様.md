## 📇 fukaya-lab-server Podman 移行 仕様書

### 1\. 目的と概要

  * **目的:**
      * Docker Compose の `include` 機能 による「構成上のグルーピング」を廃止する。
      * Podman の **Pod** 機能を導入し、「リソース（ネットワーク）を共有する実行単位」としてサービス（`OruCa`, `Entry` 等）をカプセル化する。
      * `podman kube play` を利用し、Kubernetes 互換の YAML (`pod.yml`) で Pod を宣言的に管理する。
      * ルートの `justfile` を「プロジェクト・オーケストレータ」と位置づけ、`docker-compose` コマンドの役割（全体/個別の起動・停止）を代替・強化する。

### 2\. 対象サービスとファイル構成

  * **ファイル構成の変更:**
      * 各サービスディレクトリ（`OruCa`, `Entry` 等）の `docker-compose.yml` は、`pod.yml` に置き換える。
      * ルートの `docker-compose.yml` は不要となり、`justfile` が全体管理を引き継ぐ。
  * **Pod 化対象サービス:**
      * `Entry` (caddy, coredns, adguardhome)
      * `OruCa` (web, api, mysql, nfc)
      * `ProjectBotany` (vite)
      * `homepage` (vite\_homepage)
      * `portainer`
      * `gitlab`
  * **Pod 化対象外サービス (単独コンテナ管理):**
      * `AppFlowy`: `network_mode: host` を使用するため、Pod のネットワーク分離と相容れない。これは `podman run ...` コマンドとして `justfile` で直接管理する。

### 3\. リソース管理方針

  * **ネットワーク:**
      * `fukaya-lab-network` は、`podman network create` で事前に作成する永続的な外部ネットワークとする。
      * すべての Pod は `fukaya-lab-network` に接続し、Caddy (in `Entry` Pod) からのルーティングを可能にする。
  * **ボリューム:**
      * `mysql_data`, `caddy_data` 等の名前付きボリュームは、`pod.yml` 内の `PersistentVolumeClaim` (PVC) 定義 に基づき、`podman kube play` 実行時に自動作成される。
      * Pod を `podman kube down` しても、ボリュームはデータ保護のために削除されない（`docker-compose down` と同仕様）。

### 4\. `justfile` 機能要件

ルートの `justfile` は、以下の機能を持つ必要がある。

1.  **サービスリストの管理:**
      * 起動対象とするサービス名（ディレクトリ名）のリストを変数 (`SERVICES`) として `justfile` の先頭で定義する。
      * 未完成のサービス（例: `gitlab`）は、このリストから除外（コメントアウト）することで、全体起動の対象から外せる。
2.  **YAML パスの規約:**
      * サービス名 (`{service_name}`) から YAML パス (`./{service_name}/pod.yml`) を動的に解決する規約で運用する。
3.  **全体起動・停止:**
      * `just up`: `SERVICES` リストに基づき、対象の全 Pod を `podman kube play` で起動する。
      * `just down`: `SERVICES` リストに基づき、対象の全 Pod を `podman kube down` で停止・削除する。
4.  **個別起動・停止:**
      * `just up OruCa ProjectBotany`: 引数で指定された Pod (`OruCa`, `ProjectBotany`) のみ起動する。
      * `just down OruCa`: 引数で指定された Pod のみ停止・削除する。
5.  **サービス一覧表示:**
      * `just ls`: `SERVICES` リスト（起動可能なサービス）を一覧表示する。
      * `just ps`: `podman pod ls` を実行し、現在起動中の Pod を一覧表示する。
6.  **サブコマンドの委譲:**
      * `OruCa/justfile` に定義された `backup` 等のコマンドを、ルートから呼び出せるようにする。（例: `just oruca-backup`）

-----

## 📗 fukaya-lab-server Podman 移行 定義書

### 1\. ルート `justfile` 定義

```makefile
# /justfile (ルート)

# -----------------------------------------------------------------
#  設定: プロジェクト・オーケストレーション
# -----------------------------------------------------------------

# 起動可能なサービス（Pod）のリスト。
# 未完成のサービス（例: gitlab）はコメントアウトする。
# {service_name:string, ymlpath:string} の規約を ./{{name}}/pod.yml として実装する。
SERVICES := "Entry" \
            "OruCa" \
            "homepage" \
            "portainer" \
            "ProjectBotany"
            # "gitlab"

# Podman が使用する共通ネットワーク
NETWORK := "fukaya-lab-network"

# -----------------------------------------------------------------
#  📦 サービス管理 (up, down, ls)
# -----------------------------------------------------------------

# [実行例]
#   just up               # SERVICES リストの Pod を全て起動
#   just up OruCa         # OruCa Pod のみ起動
#   just up OruCa homepage  # OruCa と homepage Pod を起動
up +services:
    @echo "==> 🚀 Starting Pods..."
    @just setup-network # ネットワークの存在確認
    @services_to_run := if argc() > 0 { services } else { SERVICES }
    @for service in services_to_run {
        if exists("./" + service + "/pod.yml") {
            echo "--> Starting {{service}}";
            podman kube play "./" + service + "/pod.yml";
        } else {
            echo "--> [!] Skipping {{service}}: pod.yml not found.";
        }
    }
    @echo "==> ✅ All requested pods started."

# [実行例]
#   just down             # SERVICES リストの Pod を全て停止・削除
#   just down OruCa       # OruCa Pod のみ停止・削除
down +services:
    @echo "==> 🛑 Stopping Pods..."
    @services_to_run := if argc() > 0 { services } else { SERVICES }
    @for service in services_to_run {
        if exists("./" + service + "/pod.yml") {
            echo "--> Stopping {{service}}";
            podman kube down "./" + service + "/pod.yml";
        } else {
            echo "--> [!] Skipping {{service}}: pod.yml not found.";
        }
    }
    @echo "==> ✅ All requested pods stopped."

# [実行例] just ls
# 起動可能なサービス（SERVICES変数）の一覧を表示
ls:
    @echo "==> 📋 Available Services (in justfile)"
    @for service in SERVICES {
        echo " - {{service}}"
    }

# [実行例] just ps
# 実際に起動している Pod の一覧を表示
ps:
    @echo "==> 🏃 Running Pods (podman pod ls)"
    @podman pod ls

# -----------------------------------------------------------------
#  🌐 ネットワーク
# -----------------------------------------------------------------
setup-network:
    @podman network exists {{NETWORK}} || (echo "==> 🌐 Creating network: {{NETWORK}}..." && podman network create {{NETWORK}})

# -----------------------------------------------------------------
#  🛠️ サービス固有コマンド (サブ justfile の呼び出し)
# -----------------------------------------------------------------

# [実行例] just oruca-backup
# OruCa/justfile の backup タスクを実行する
oruca-backup:
    @echo "==>  delegating to OruCa/justfile..."
    @just --justfile "./OruCa/justfile" backup

# [実行例] just oruca-restore
oruca-restore:
    @echo "==> delegating to OruCa/justfile..."
    @just --justfile "./OruCa/justfile" restore

# -----------------------------------------------------------------
#  🖥️ Pod化対象外 (AppFlowy)
# -----------------------------------------------------------------
# network_mode: host のため Pod にしない
appflowy-up:
    @echo "==> 🚀 Starting AppFlowy (non-Pod)..."
    @podman run -d --rm --name appflowy \
        --network=host \
        -e DISPLAY=$DISPLAY \
        -e NO_AT_BRIDGE=1 \
        -v $HOME/.Xauthority:/root/.Xauthority:rw \
        -v /tmp/.X11-unix:/tmp/.X11-unix \
        -v /dev/dri:/dev/dri \
        -v /var/run/dbus/system_bus_socket:/var/run/dbus/system_bus_socket \
        --device /dev/dri \
        appflowy/appflowy:latest

appflowy-down:
    @echo "==> 🛑 Stopping AppFlowy..."
    @podman stop appflowy
```

### 2\. `pod.yml` 定義 (テンプレート例: OruCa)

`OruCa/docker-compose.yml` を Pod 化した例です。

  * `oruca-mysql` と `oruca-api` 等は `localhost` で通信できます。
  * `mysql_data` は `PersistentVolumeClaim` (PVC) として定義します。
  * `fukaya-lab-network` に接続します。
  * Caddy が検知するためのラベル `caddy: http://oruca.fukaya-sus.lab` は、Pod ではなく `oruca-web` コンテナに付与します（Caddy Docker Proxy はコンテナのラベルを見るため）。

<!-- end list -->

```yaml
# /OruCa/pod.yml (新規作成)

apiVersion: v1
kind: Pod
metadata:
  name: oruca-pod # Pod 全体の名前
  labels:
    project: "fukaya-lab-server" # 管理用の共通ラベル
spec:
  # Pod を fukaya-lab-network に接続する
  networks:
    - name: fukaya-lab-network
      # このネットワークは事前に podman network create で作成しておく

  # Pod として公開するポート (開発用)
  portMappings:
    - containerPort: 4000
      hostPort: 4000
      protocol: tcp

  # Pod 内のコンテナ群
  containers:
    # -----------------------------------------------
    #  OruCa Vite (dev)
    # -----------------------------------------------
    - name: oruca-vite
      image: oruca-vite:latest # (事前に podman build -t oruca-vite ./vite が必要)
      volumeMounts:
        - mountPath: /app
          name: oruca-vite-src
      # 開発時は 4000 ポートを使う
      command: ["tail", "-f", "/dev/null"]
      # (補足) profiles: dev の再現は、dev用の oruca-dev-pod.yml を別途作るのが一般的

    # -----------------------------------------------
    #  OruCa Web (prod)
    # -----------------------------------------------
    - name: oruca-web
      image: oruca-web:latest # (事前に podman build -t oruca-web ./web が必要)
      volumeMounts:
        - mountPath: /usr/share/nginx/html
          name: oruca-vite-dist
      # caddy がリバースプロキシするポートは 80
      expose:
        - 80
      # caddy-docker-proxy はコンテナのラベルを見る
      labels:
        caddy: http://oruca.fukaya-sus.lab
        caddy.reverse_proxy: "{{upstreams 80}}"
      dependsOn: ["oruca-api"] # Pod内の起動順序制御 (API -> Web)

    # -----------------------------------------------
    #  OruCa API
    # -----------------------------------------------
    - name: oruca-api
      image: oruca-api:latest # (事前に podman build -t oruca-api ./api が必要)
      env:
        # DBへの接続は 'oruca-mysql' ではなく 'localhost' または '127.0.0.1' を使う
        - name: DB_HOST
          value: "127.0.0.1" 
        - name: MYSQL_DATABASE
          valueFrom:
            secretKeyRef:
              name: oruca-secrets
              key: MYSQL_DATABASE
        # (他の env も同様に secretKeyRef や configMapKeyRef を推奨)
      volumeMounts:
        - mountPath: /app
          name: oruca-api-src
      command: ["yarn", "start"]
      dependsOn: ["oruca-mysql"] # Pod内の起動順序制御 (MySQL -> API)

    # -----------------------------------------------
    #  OruCa MySQL
    # -----------------------------------------------
    - name: oruca-mysql
      image: oruca-mysql:latest # (事前に podman build -t oruca-mysql ./mysql が必要)
      env:
        # (env は secretKeyRef を推奨)
      capAdd:
        - "SYS_NICE"
      volumeMounts:
        - mountPath: /docker-entrypoint-initdb.d/init.sql
          name: oruca-mysql-init-sql
          subPath: init.sql
        - mountPath: /var/lib/mysql
          name: mysql-data-pvc # PVC をマウント
        - mountPath: /backups
          name: oruca-mysql-backups
      healthcheck:
        test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
        # (healthcheck の他パラメータもここに記述)

    # -----------------------------------------------
    #  OruCa NFC
    # -----------------------------------------------
    - name: oruca-nfc
      image: oruca-nfc:latest # (事前に podman build -t oruca-nfc ./nfc が必要)
      init: true
      volumeMounts:
        - mountPath: /app
          name: oruca-nfc-src
      env:
        - name: DB_HOST
          value: "127.0.0.1" # API と同様に localhost で接続
        # (他の env)
      command: ["python", "main.py"]
      dependsOn: ["oruca-mysql"] # Pod内の起動順序制御
      # USB デバイスを Pod に渡す設定
      securityContext:
        devices:
          - /dev/bus/usb:/dev/bus/usb

  # Pod が使用するボリュームの定義
  volumes:
    # バインドマウント (hostPath)
    - name: oruca-vite-src
      hostPath:
        path: ./vite
        type: Directory
    - name: oruca-vite-dist
      hostPath:
        path: ./vite/dist
        type: Directory
    - name: oruca-api-src
      hostPath:
        path: ./api
        type: Directory
    - name: oruca-mysql-init-sql
      hostPath:
        path: ./mysql/data/init.sql
        type: File
    - name: oruca-mysql-backups
      hostPath:
        path: ./mysql/backups
        type: Directory
    - name: oruca-nfc-src
      hostPath:
        path: ./nfc
        type: Directory

    # 名前付きボリューム (PersistentVolumeClaim)
    - name: mysql-data-pvc
      persistentVolumeClaim:
        claimName: mysql_data # docker-compose のボリューム名
```