# /fukaya-lab-server/.env.example 

# ---------------------------------
# ネットワーク設定 (Entry サービス用)
# ---------------------------------
# サーバーのホストIPアドレス (CoreDNSがバインドするIP)
# 開発時は 127.0.0.1 や 開発PCのIP を設定
# HOST_IP=192.168.11.224
HOST_IP=127.0.0.1

# Corefile で参照されるIP (ssh.fukaya-sus.lab など)
# 開発時は 127.0.0.1 や 開発PCのIP を設定
# SSH_IP=192.168.11.225
SSH_IP=127.0.0.1

# CoreDNS がホスト側で待ち受けるポート
# 開発PCで53番が競合する場合は 1053 などに変更する
COREDNS_PORT=53

# ---------------------------------
# Portainer 設定
# ---------------------------------
# Portainerのデータを保存するホストディレクトリ
PORTAINER_DATA_DIR=./data/portainer