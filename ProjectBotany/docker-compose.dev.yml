# docker-compose.dev.yml (Development overrides)
services:
  vite:
    container_name: vite_dev
    build:
      target: dev  # Dockerfileのdevステージを使用
    ports:
      - "5173:5173"
    tty: true
    stdin_open: true
