services:
  # 🔄 Caddy - Reverse Proxy
  caddy:
    image: lucaslorentz/caddy-docker-proxy:2.9-alpine
    container_name: proxy-caddy
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    networks:
      -  fukaya-lab-network
    environment:
      - CADDY_INGRESS_NETWORKS=fukaya-lab-network
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - caddy_data:/data
    # labels:
    #   # グローバルオプション：全サイトで内部CAを有効にする (tls internal と同義)
    #   caddy.local_certs: 
    #   # グローバルオプション：ワイルドカード証明書が利用可能な場合、優先して使用する
    #   caddy.auto_https: prefer_wildcard      
    #   # ダミーのサイトブロックを定義し、ワイルドカード証明書をCaddyに発行させる
    #   caddy_1: "http://*.fukaya-sus.lab"
    #   caddy_1.respond: "Wildcard Fallback 200" # 👈 万が一アクセスされた場合の応答

  # 👑 CoreDNS - Primary DNS Resolver
  coredns:
    image: coredns/coredns:latest
    container_name: gatewaydns-coredns
    restart: unless-stopped
    ports:
      # IPとポートを環境変数から読み込む
      - "${HOST_IP}:${COREDNS_PORT}:53/tcp"
      - "${HOST_IP}:${COREDNS_PORT}:53/udp"
    networks:
      - fukaya-lab-network
    volumes:
      - ./Corefile:/Corefile
    environment:
      # Corefile 内で $HOST_IP と $SSH_IP を使用可能にする
      - HOST_IP=${HOST_IP}
      - SSH_IP=${SSH_IP}
    depends_on:
      - adguardhome
      - caddy

  # 🛡️ AdGuard Home - DNS Server
  adguardhome:
    image: adguard/adguardhome:latest
    container_name: maindns-adguardhome
    restart: unless-stopped
    ports: [] # Caddy経由でのみ公開するためホストポートは不要
    networks:
      fukaya-lab-network:
        ipv4_address: 172.20.0.11 # 👈 AdGuard Homeの固定IP
    volumes:
      - adguard_work:/opt/adguardhome/work
      - adguard_conf:/opt/adguardhome/conf
    labels:
      caddy: http://dns.fukaya-sus.lab
      caddy.reverse_proxy: "{{upstreams 3000}}"
      # caddy.tls: "internal"