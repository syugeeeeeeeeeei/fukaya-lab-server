# /Services/portainer/justfile (修正後)

_default:
  @just --list -u

[doc("portainer サービスはビルド不要です（既存イメージ利用）。")]
build:
    @echo "==> ℹ️ Building portainer (if images defined)..."
    @podman compose --env-file ../../.env build

[doc("portainer の Pod (pod.yml) を起動します。")]
up:
    @echo "--> 🚀 Starting portainer"
    @podman compose --env-file ../../.env up -d

[doc("portainer の Pod (pod.yml) を停止します。")]
down:
    @echo "--> 🛑 Stopping portainer"
    @podman compose --env-file ../../.env down