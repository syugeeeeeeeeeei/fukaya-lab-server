# ProjectBotany/docker-compose.yml
name: projectbotany
services:
  # -----------------------------------------------------------
  #  🌿 ProjectBotany
  # -----------------------------------------------------------
  projectbotany-vite:
    build:
      context: ./vite
      dockerfile: Dockerfile
    container_name: projectbotany-vite
    volumes:
      - ./vite:/app:delegated
      - /app/node_modules
    networks:
      - fukaya-lab-network
    restart: unless-stopped
    command: ["yarn", "dev"]
    labels:
      caddy: http://projectbotany.app.fukaya-sus.lab # ドメイン名
      caddy.reverse_proxy: "{{upstreams 5173}}"

networks:
  fukaya-lab-network:
    external: true
    name: fukaya-lab-network