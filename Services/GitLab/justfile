# /Services/gitlab/justfile (修正後)

_default:
  @just --list -u

[doc("gitlab サービスはビルド不要です（既存イメージ利用）。")]
build:
    @echo "==> ℹ️ Building gitlab (if images defined)..."
    @podman compose --env-file ../../.env build

[doc("gitlab の Pod (pod.yml) を起動します。")]
up:
    @echo "--> 🚀 Starting gitlab"
    @podman compose --env-file ../../.env up -d

[doc("gitlab の Pod (pod.yml) を停止します。")]
down:
    @echo "--> 🛑 Stopping gitlab"
    @podman compose --env-file ../../.env down